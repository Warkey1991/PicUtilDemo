<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cxkr.picutildemo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".simpic.SimPicActivity"/>
        <activity android:name=".album.AlbumsActivity"/>
        <activity android:name=".album.AlbumItemActivity"/>
        <activity android:name=".album.SelectedImagesActivity"/>
        <activity android:name=".recycle.RecyclerPictureActivity"/>
        <activity android:name=".BigPicActivity"/>
        <activity android:name=".blurry.BlurryPicActivity"/>
        <activity android:name=".blurry.BlurryPictureActivity"/>

    </application>

</manifest>